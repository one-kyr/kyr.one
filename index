<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Contact Kyr Adelerhof - kyr.one">
    <title>Contact Kyr Adelerhof | kyr.one</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --text-color: #fff;
            --accent-color: #8a2be2;
            --glass-bg: rgba(255, 255, 255, 0.1);
            --shadow-color: rgba(0, 0, 0, 0.4);
        }

        html, body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', sans-serif;
            color: var(--text-color);
            background: url('image2.jpg') no-repeat center center fixed;
            background-size: cover;
            width: 100%;
            height: 100%;
            overflow: hidden;
            perspective: 2000px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        body::before {
            content: '';
            position: absolute;
            inset: 0;
            z-index: -1;
            background: inherit;
            filter: blur(0px);
        }

        .glass-container {
            background: var(--glass-bg);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border-radius: 24px;
            box-shadow: 0 16px 48px var(--shadow-color);
            padding: 3rem;
            width: 90%;
            max-width: 700px;
            text-align: center;
            transform-style: preserve-3d;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            will-change: transform, box-shadow;
        }

        h1 {
            margin-bottom: 0.5rem;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.5);
        }

        p {
            font-size: 1.2rem;
        }

        .btn {
            background-color: var(--accent-color);
            color: #fff;
            padding: 0.9rem 1.7rem;
            border-radius: 16px;
            text-decoration: none;
            box-shadow: 0 10px 25px var(--shadow-color);
            transition: transform 0.3s ease;
            display: inline-block;
            margin-top: 1.5rem;
        }

        .btn:hover {
            transform: translateY(-5px);
        }

        @media (max-width: 768px) {
            .glass-container {
                margin: 1rem;
                padding: 2rem;
            }
        }
    </style>
</head>
<body>

<div class="glass-container">
    <h1>Contact Kyr Adelerhof</h1>
    <p>Feel free to reach out via email for inquiries, collaboration, or just to say hello!</p>
    <p>Email: <a href="mailto:k@kyr.one" style="color: inherit;">k@kyr.one</a></p>
    <p>Location: Borculo, Netherlands</p>
    <a class="btn" href="mailto:k@kyr.one">Send Email</a>
</div>

<script>
    const container = document.querySelector('.glass-container');

    container.addEventListener('mousemove', e => {
        const rect = container.getBoundingClientRect();
        const x = ((e.clientX - rect.left) / rect.width - 0.5) * 20;
        const y = ((e.clientY - rect.top) / rect.height - 0.5) * -20;
        container.style.transform = `rotateY(${x}deg) rotateX(${y}deg) scale(1.03)`;
        container.style.boxShadow = `${-x}px ${y}px 60px var(--shadow-color)`;
    });

    container.addEventListener('mouseleave', () => {
        container.style.transform = 'rotateY(0deg) rotateX(0deg) scale(1)';
        container.style.boxShadow = `0 16px 48px var(--shadow-color)`;
    });
</script>

</body>
</html>